# Referencias oficiales consultadas

- Claude Code: memoria de proyecto y `CLAUDE.md`.
- Claude Code: creación y distribución de skills mediante `SKILL.md`.
- Amazon Bedrock: Converse API.
- Amazon Bedrock: tool use con Converse API.
- AWS API Gateway + Lambda + DynamoDB.
- AWS Lambda: buenas prácticas.
- AWS SAM: plantillas, `sam build` y `sam deploy` (infraestructura del MVP, ADR-003).

Verificar nuevamente la documentación oficial antes de implementar servicios o sintaxis que puedan haber cambiado.
