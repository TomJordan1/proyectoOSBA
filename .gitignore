# === Secretos y entorno ===
.env
.env.*
!.env.example
*.pem
samconfig.toml

# === Artefactos de build ===
**/bin/
**/obj/
**/node_modules/
**/dist/
**/.aws-sam/
cdk.out/
coverage/

# === IDE / sistema ===
.vs/
.idea/
*.user
*.suo
*.log
.DS_Store

# === Interno / sensible: NO publicar en GitHub ===
# Configuración y skills del agente
.claude/
CLAUDE.md
# Hojas de ruta, historial y notas de avance (uso interno)
HOJA_RUTA.md
HOJA_RUTA_B2B2E.md
HISTORIAL_CAMBIOS.md
NEXT_SESSION.md
# Prompts y material de reestructuración interno
PROMPT_*.md
00_LEER_PRIMERO.md
Reestruturacion/
# Empaquetados
*.zip

# Config de distribución del agente (contiene API key)
kandace.settings.json
**/kandace.settings.json

# Frases personalizadas locales (el ejemplo sí se versiona)
kanny.frases.json
**/kanny.frases.json

# Publicados / instaladores / binarios grandes (van a S3, NO al repo)
**/publish/
**/installer/Output/
*.exe

# Archivos temporales de pruebas AWS (definiciones de ítems / políticas)
item.json
key.json
demo.json
jurado.json
policy.json
kandace_logs*.txt

# Webs legadas locales (reemplazadas por web/ = Proyecto_uiinterface). No subir.
laminar-web/
landing/
