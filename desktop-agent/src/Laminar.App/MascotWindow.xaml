<Window x:Class="Laminar.App.MascotWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Laminar.App"
        Title="Laminar" Width="190" Height="210"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        MouseLeftButtonDown="OnDrag" MouseDoubleClick="OnOpenConfig">
    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem Header="Abrir configuración" Click="OnOpenConfig"/>
            <MenuItem Header="Ocultar acompañante" Click="OnHide"/>
        </ContextMenu>
    </Window.ContextMenu>

    <Grid>
        <!-- Globo de mensaje (oculto por defecto) -->
        <Border x:Name="Bubble" Visibility="Collapsed" VerticalAlignment="Top" HorizontalAlignment="Center"
                Background="#161C2A" CornerRadius="10" Padding="10,6" Margin="0,0,0,6" MaxWidth="170" Panel.ZIndex="2">
            <TextBlock x:Name="BubbleText" Foreground="#E6EDF3" FontSize="11" TextWrapping="Wrap"/>
        </Border>

        <!-- Kanny: dibujo inmediato (KannyView), reacciona al estrés real -->
        <local:KannyView x:Name="Kanny" Width="180" Height="180" BaseRadius="52"
                         ParticleCount="20" TargetFps="30"
                         VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
    </Grid>
</Window>
